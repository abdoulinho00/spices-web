spring:
  datasource:
    name: spices
    url: jdbc:mysql://localhost:3306/spices
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: MYSQL

  http:
    multipart:
      max-file-size: 25MB
      max-request-size: 30MB

file-manager:
  base-folder:  /Users/dev/workspace/spices-web/data